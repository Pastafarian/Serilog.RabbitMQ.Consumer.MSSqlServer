using Microsoft.AspNetCore.Mvc.RazorPages;

namespace IntegrationTest.WebApplication.Pages
{

    public class SetupTwoModel : PageModel
    {
        //private readonly IPerson _person;

        //public SetupTwoModel(IPerson person, IAddress address)
        //{
        //    _person = person;

        //    if (person.Address.AddressLineOne != address.AddressLineOne)
        //    {
        //        throw new Exception("Invalid person");
        //    }

        //    if (person.Name != new PrimeMinister(new BuckinghamPalace()).Name)
        //    {
        //        throw new Exception("Invalid person");
        //    }

        //    if (person.Address.AddressLineOne != new PrimeMinister(new BuckinghamPalace()).Address.AddressLineOne)
        //    {
        //        throw new Exception("Invalid person");
        //    }
        //}

        public void OnGet()
        {
        }
    }
}
